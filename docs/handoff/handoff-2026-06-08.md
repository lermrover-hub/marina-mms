# Marina MMS — Session Handoff 2026-06-08

## Status: Ready for next Codex session

---

## What was completed this session

### 1. W-wording fix (production DB)
- 3 pricing_master rows had W-berth/W-slots wording → fixed to "wet berth"
- Rule: **W = Workshop** (not Wet Berth)
- Confirmed in production

### 2. Pilot quotation 07b75681 — zinc anode correction
- Yard supervisor confirmed: 22 zinc anodes, 8 labour hours
- Production write approved and executed
- Quotation total: THB 57,495, status: DRAFT
- Customer "Complete Marine Services Co., Ltd." has no delivery channel — quotation CANNOT be sent until email/LINE/WhatsApp is added to customer record

### 3. AI agent integration review
- Fixed `full_name` bug in quotation route (no such column — use `first_name`+`last_name`)
- Fixed `test:live` script gate (`--env-file=.env.test.live`)
- File: `app/api/db/quotations/[id]/route.ts` — SENT guard working correctly

### 4. Official document template system
All print pages now use shared Palm Beach Samui Asset branding.

**New files:**
- `components/print/OfficialDocumentShell.tsx` — shared shell, company constants, `PBLogo`, `PBWatermark`, `PBCompanyHeader`, `ESignBlock`, `OfficialDocumentShell`
- `components/print/DocumentField.tsx` — reusable field helpers

**Updated pages:**
- `app/print/quotations/[id]/page.tsx`
- `app/print/invoices/[id]/page.tsx`
- `app/print/contracts/[id]/page.tsx`
- `app/print/ramp-bookings/[id]/page.tsx`
- `app/print/work-orders/[id]/page.tsx`
- `app/(dashboard)/documents/berthing-form/page.tsx`
- `app/(dashboard)/documents/hardstand-form/page.tsx`

**Logo assets (still needed — manual step):**
Place these files at `public/document-assets/`:
- `header logo 2.jpeg` — header logo, top-left on all documents
- `watermark logo.png` — background watermark at 5% opacity

Until uploaded, the text fallback logo shows automatically.

---

## Company constants (already in code)

```
บริษัท ปาล์มบีช สมุย แอสเสท จำกัด (สำนักงานใหญ่)
Palm Beach Samui Asset Co., Ltd.
26/24 ม.4 ต.แม่น้ำ อ.เกาะสมุย จ.สุราษฎร์ธานี 84330
เลขประจำตัวผู้เสียภาษี: 0845558004072
Tel: 094-4563966
```

---

## Pending items for next session

### P1 — Quotation send unblock
- Customer record `ea35b2fb-...` (Complete Marine Services Co., Ltd.) needs email or LINE or WhatsApp added before quotation 07b75681 can be sent
- Verify `customer_id` on quotation matches correct customer record

### P2 — Logo asset upload
- Upload `header logo 2.jpeg` and `watermark logo.png` to `public/document-assets/`
- Vercel deploy will pick them up automatically

### P3 — git push
- Last committed: `5887e76 fix: complete official print watermark and e-sign`
- Push to origin when ready

### P4 — Customer portal (Phase 3)
- Not started — lower priority until core modules are stable

---

## Active rules (never change without explicit approval)

- W = Workshop (not Wet Berth)
- Do not enable production writes without explicit approval
- Do not send quotation 07b75681 to customer — blocked (no delivery channel)
- Do not change pricing_master or rate card
- No secrets in commits
- AI_AGENT_DRY_RUN=true (default)
- AI_AGENT_SKIP_CLAUDE=true (during tests)

---

## Key file locations

| File | Purpose |
|------|---------|
| `components/print/OfficialDocumentShell.tsx` | Shared print shell + company constants |
| `components/print/DocumentField.tsx` | Reusable field helpers |
| `app/print/quotations/[id]/page.tsx` | Quotation print page |
| `app/print/invoices/[id]/page.tsx` | Invoice print page |
| `app/api/db/quotations/[id]/route.ts` | Quotation API (SENT guard, approve action) |
| `ai-agents/` | AI agent team (read-only by default) |
| `ai-agents/.env.test.live` | Enable live API tests (gitignored) |
| `public/document-assets/` | Logo assets directory (drop files here) |
| `DEV_SCRIPTS.md` | Session notes and correction log |
| `docs/handoff/handoff-2026-06-08.md` | This file |

---

## Test commands

```powershell
# TSC check
& "C:\marina-mms\node_modules\.bin\tsc.cmd" --project "C:\marina-mms\tsconfig.json" --noEmit

# Agent tests (offline)
node --test C:\marina-mms\ai-agents\tests\*.test.js

# Agent tests (live staging)
node --env-file=C:\marina-mms\ai-agents\.env.test.live --test C:\marina-mms\ai-agents\tests\*.test.js

# Git push
git -C C:\marina-mms push
```
